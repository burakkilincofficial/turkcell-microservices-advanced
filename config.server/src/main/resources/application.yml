spring:
  application:
    name: config.server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/halitkalayci/advanced-configurations.git
          default-label: master
          search-paths: .
          #clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8787